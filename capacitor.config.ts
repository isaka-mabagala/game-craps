import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isaxstar.gamecraps',
  appName: 'game-craps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
